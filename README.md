# infohub
git clone https://github.com/pechetti-16/infohub.git

cd infohub/backend

npm install

Create .env with MONGO_URI, JWT_SECRET, PORT

npm run dev (or node index.js)

cd ../mini1 (frontend)

npm install && npm start
